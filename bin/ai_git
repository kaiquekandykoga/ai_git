#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ai_git"

begin
  AIGit.start(ARGV)
rescue Interrupt
  warn "\nAborted."
  exit 130
rescue StandardError => e
  warn "ai_git: #{e.message}"
  exit 1
end
